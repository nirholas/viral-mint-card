import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const concept=JSON.parse(readFileSync('concept.json','utf8'));assert.equal(concept.transactionVersion,1);assert.equal(concept.maxTransactionBytes,4096);assert.ok(concept.description.length>40);const app=readFileSync('app.js','utf8');assert.match(app,/maxSupportedTransactionVersion|supportedTransactionVersions|transactionConfig|v1/);console.log('Product contract verified:',concept.slug);
